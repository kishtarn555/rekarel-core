class program {
    program() {
        iterate(16) {
            turnleft();
            if (facingNorth) {
                continue;
            }
            putbeeper();
       }
        turnoff();
    }
}