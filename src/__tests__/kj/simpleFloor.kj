import rekarel.globals;

class program {    
    void clone(n) {
        move();
        iterate(n) putbeeper();
    }
    program() {
        clone(beepersOnFloor);
        move();
        //Same as while nextToABeeper
        while (!iszero(beepersOnFloor)) {
            pickbeeper();
        }
        turnoff();
    }
}