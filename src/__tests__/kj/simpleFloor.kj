import rekarel.globals;

class program {    
    void clone(n) {
        move();
        iterate(n) putbeeper();
    }
    program() {
        clone(floorBeepers);
        move();
        //Same as while nextToABeeper
        while (!iszero(floorBeepers)) {
            pickbeeper();
        }
        turnoff();
    }
}